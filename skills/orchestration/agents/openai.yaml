interface:
  display_name: "Orchestra"
  short_description: "Strategy-first orchestration with context policy"
  default_prompt: "Use $orchestra:orchestration to choose a strategy and explicit native fork_turns before task tools, send compact evidence-scoped packets, then deliver and verify the result."
