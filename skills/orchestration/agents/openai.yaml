interface:
  display_name: "Orchestra"
  short_description: "Adaptive single-agent-first orchestration"
  default_prompt: "Use $orchestra:orchestration to keep connected work with one owner by default, add agents only for measured value, and verify with compact artifact handoffs."
